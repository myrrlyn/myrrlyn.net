<!DOCTYPE html>
<!--  |
    ——0—— REGISTERED BY C0DA.
      |   -->
<html>
	<head>
		<title>
			<% if current_page.data.title %>
<%= current_page.data.title %>
			<% else %>
oeuvre myrrlyn
			<% end %>
		</title>
		<%= stylesheet_link_tag :oeuvre %>
		<%= partial "layouts/head" %>
		<%= javascript_include_tag :oeuvre %>
	</head>
	<body class="<%= page_classes %> flex-wrapper flex-rows">
		<%= partial "layouts/header" %>
		<div id="flex-focus"
		     class="flex-wrapper flex-cols">
			<main class="flex-item">
				<h1 class="title">
					<%= current_page.data.title %>
				</h1>
				<article>
					<%= yield %>
				</article>
			</main>
			<%= partial "layouts/oeuvre/about" %>
			<%= partial "layouts/oeuvre/nav" %>
		</div>
		<%= partial "layouts/footer" %>
	</body>
</html>
