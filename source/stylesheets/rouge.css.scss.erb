<%= Rouge::Themes::ThankfulEyes.render(:scope => '.highlight') %>

@mixin tabs($width) {
	table pre {
		tab-size: $width;
	}
}

.highlight {
	@include tabs(4);

	//  For some reason, C is marked italic
	&.c {
		font-style: normal;
	}

	&.ruby {
		@include tabs(2);
	}
}
